{
  "name": "pi-extensions",
  "version": "0.1.0",
  "private": true,
  "description": "Personal pi extension collection: /exit, /plan, ...",
  "type": "module",
  "license": "MIT",
  "pi": {
    "extensions": [
      "./packages/exit/index.ts",
      "./packages/plan-mode/index.ts"
    ]
  },
  "scripts": {
    "check": "bun build --no-bundle packages/exit/index.ts >/dev/null && bun build --no-bundle packages/plan-mode/index.ts >/dev/null && node --test 'packages/**/*.test.mjs'",
    "test": "node --test 'packages/**/*.test.mjs'"
  }
}
