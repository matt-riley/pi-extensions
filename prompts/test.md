---
description: Write tests for a file, following existing patterns
argument-hint: "<path>"
---
Write tests for $1. Match the repo's existing test framework, file location, and naming conventions. Cover the edge cases, not just the happy path. Run the tests and report the result.
